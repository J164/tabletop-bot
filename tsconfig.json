{
  "compilerOptions": {
    // Type Checking
    "exactOptionalPropertyTypes": true,
    "strict": true,

    // Modules
    "module": "NodeNext",

    // Emit
    "outDir": "build",
    "sourceMap": true,

    // Language and Environment
    "target": "ES2022",
  },
  "include": ["src/**/*.ts"]
}